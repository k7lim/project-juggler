i want a CLI that reads the locally stored files for a variety of agents, and provides an interface to review the relevant behavior and history with that agentic tool: to browse, resume, copy-paste from that agent transcripts or settings.

for example, claude code, the one i use the most has 
~/.claude/history.jsonl and ~/.claude/projects/<encoded-path>/<sessionId>.jsonl

i am curious what else this tool stores in .claude

but also what other tools (codex, opencode, pi, hermes, amp) store locally that we can surface using a CLI tool.
